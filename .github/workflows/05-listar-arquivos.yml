name: 05 - Listar Arquivos

on:
  push: 
    branches: [ main ]

jobs: 
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4 # baixa/acessa o código para a máquina do action

      - name: Listar Arquivos
        run: |
          echo "Lista de arquivos:"
          ls -R
      
